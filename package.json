{
  "name": "par-herdr-plugins",
  "version": "0.1.0",
  "private": true,
  "description": "Custom Herdr plugins (monorepo).",
  "license": "MIT",
  "workspaces": ["plugins/*"],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint .",
    "fmt": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
